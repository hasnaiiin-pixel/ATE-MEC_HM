const fs=require('fs'); const path=require('path'); const root=process.cwd();
function exists(p){return fs.existsSync(path.join(root,p));}
function electronBinaryExists(){return exists('node_modules/electron/dist/electron.exe')||exists('node_modules/.bin/electron')||exists('node_modules/.bin/electron.cmd');}
const checks=[]; function add(label,ok,detail=''){checks.push({label,ok:!!ok,detail});}
add('package.json',exists('package.json'),'root'); add('dist/main/main.js',exists('dist/main/main.js'),'electron main'); add('renderer index',exists('src/renderer/index.html'),'ui'); add('config users',exists('config/users.json'),'Admin/Tecnico'); add('database clean',exists('database/ate_mec_local_db.json')&&exists('database/ate_mec_enterprise_db.json'),'json db'); add('drivers folder',exists('drivers/INSTALLA_DRIVER_HARDWARE_10.0.bat'),'drivers optional'); add('electron',electronBinaryExists(),'node_modules');
const critical=!exists('package.json')||!exists('dist/main/main.js')||!exists('src/renderer/index.html')||!exists('config/users.json'); const electronMissing=!electronBinaryExists(); const report={version:'AT-MEC_HM_10.0_ENTERPRISE_STABLE_CLEAN_PRODUCTION_WITH_DRIVERS',createdAt:new Date().toISOString(),checks,criticalMissing:critical,electronMissing}; fs.mkdirSync(path.join(root,'docs/quality'),{recursive:true}); fs.writeFileSync(path.join(root,'docs/quality/AT_MEC_HM_10_0_STARTUP_DOCTOR.json'),JSON.stringify(report,null,2)); console.log('AT-MEC_HM_10.0 startup doctor'); checks.forEach(c=>console.log(`${c.ok?'OK  ':'WARN'} ${c.label}${c.detail?' - '+c.detail:''}`)); if(critical)process.exit(2); if(electronMissing)process.exit(3); process.exit(0);
